// zhihu-radar CLI entry.
//
// Scaffolding only — no commands wired yet. Kept empty on purpose so that
// `pnpm check` passes on a fresh clone without any business code.
// The first real command will go here (or be imported from a command
// module inside runtime/).

export {};
